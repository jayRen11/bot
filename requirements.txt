# ==========================================
# 1. 核心 Web 框架
# ==========================================
streamlit

# ==========================================
# 2. AI 引擎与向量检索
# ==========================================
openai>=1.35.0
httpx>=0.27.0
sentence-transformers
torchvision
chromadb

# 🌟【云端自动补丁核心】：解决 ChromaDB 在云端的 SQLite 版本过低问题
# 使用环境标记：只在 Linux (云端) 下载安装，Windows 本地直接忽略，不污染本地环境
pysqlite3-binary; sys_platform == 'linux'

# ==========================================
# 3. 多模态解析 (OCR、文档、图片)
# ==========================================
pdfplumber
python-docx
pytesseract
pdf2image
Pillow

# ==========================================
# 4. 语音交互组件
# ==========================================
streamlit-mic-recorder
gtts

# ==========================================
# 5. 底层依赖降级与冲突解决
# ==========================================
# 防止 Tensorboard/TF 依赖冲突
protobuf<=3.20.3
# 🌟【网络底层补丁】：防止 Streamlit Cloud (Debian) 与 HuggingFace 发生 SSL 握手崩溃 (OSError)
urllib3<2.0.0
